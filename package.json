{
  "name": "directus-starter",
  "version": "1.0.0",
  "description": "Directus template for railway app",
  "scripts": {
    "start": "npx directus start",
    "create-snapshot": "npx directus schema snapshot --yes ./snapshot.yaml",
    "apply-snapshot": "npx directus schema apply --yes ./snapshot.yaml"
  },
  "engines": {
    "node": ">=12.20.0"
  },
  "dependencies": {
    "directus": "^9.26.0",
    "pg": "^8.9.0"
  }
}
